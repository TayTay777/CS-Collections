#include <stdio.h>
#include <stdlib.h>
#include "mystrcpy.h"


char** myRevArr(char** firArr, int size){

**firArr = (char**)malloc(numStrings * sizeof(char*));

for (int i = size; i > 0; --i){


}



return NULL;

}
